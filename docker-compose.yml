services:
  api:
    container_name: little-notes-api
    build:
      context: ./api
    ports:
      - ${API_PORT}:${API_PORT}
    volumes:
      - ./api:/app
    environment:
      - NODE_ENV=${NODE_ENV}
      - PORT=${API_PORT}
